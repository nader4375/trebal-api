// Placeholder constitution module so the API can boot.
// We will replace this with the full Trebal constitution logic later.

export const constitution = {
  version: "draft",
  steps: [],
};

export default constitution;
